import { NextResponse } from 'next/server'; import { createClient } from '@/lib/supabase/server';
export async function GET(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Connexion requise.'},{status:401});const {data,error}=await s.from('crm_leads').select('*').eq('owner_id',user.id).order('updated_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data:data??[]});}
export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Connexion requise.'},{status:401});const b=await req.json();if(!String(b.name||'').trim())return NextResponse.json({error:'Nom du prospect requis.'},{status:400});const {data,error}=await s.from('crm_leads').insert({owner_id:user.id,contact_user_id:b.contact_user_id||null,listing_id:b.listing_id||null,name:String(b.name),phone:b.phone||null,email:b.email||null,source:b.source||'winswi',stage:b.stage||'new',score:Number(b.score||0),notes:b.notes||''}).select('*').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data},{status:201});}
export async function PATCH(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Connexion requise.'},{status:401});const b=await req.json();if(!b.id)return NextResponse.json({error:'id requis.'},{status:400});const allowed={name:b.name,phone:b.phone,email:b.email,stage:b.stage,score:b.score,notes:b.notes,next_action_at:b.next_action_at};const {data,error}=await s.from('crm_leads').update(allowed).eq('id',b.id).eq('owner_id',user.id).select('*').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data});}
