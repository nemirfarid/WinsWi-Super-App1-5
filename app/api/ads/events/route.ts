import { NextResponse } from 'next/server'; import { createClient } from '@/lib/supabase/server';
export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();const b=await req.json();if(!b.campaign_id||!b.event_type)return NextResponse.json({error:'Paramètres invalides.'},{status:400});const {error}=await s.from('ad_events').insert({campaign_id:b.campaign_id,event_type:b.event_type,viewer_id:user?.id??null,metadata:b.metadata||{}});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
