import { NextResponse } from 'next/server'; import { createClient } from '@/lib/supabase/server';
async function admin(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return {s,user:null,ok:false};const {data:p}=await s.from('profiles').select('role').eq('id',user.id).single();return {s,user,ok:p?.role==='admin'}}
export async function GET(){const {s,ok}=await admin();if(!ok)return NextResponse.json({error:'Accès administrateur requis.'},{status:403});const {data,error}=await s.from('reports').select('*').order('created_at',{ascending:false}).limit(200);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({data:data??[]})}
export async function PATCH(req:Request){const {s,ok}=await admin();if(!ok)return NextResponse.json({error:'Accès administrateur requis.'},{status:403});const b=await req.json().catch(()=>({}));if(typeof b.id!=='string'||!['open','reviewing','resolved','dismissed'].includes(b.status))return NextResponse.json({error:'Paramètres invalides.'},{status:400});const {data,error}=await s.from('reports').update({status:b.status}).eq('id',b.id).select().single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data})}
