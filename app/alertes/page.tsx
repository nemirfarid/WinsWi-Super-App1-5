'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {universes} from '@/lib/data';
export default function Alerts(){const [items,setItems]=useState<any[]>([]);const [title,setTitle]=useState('');const [universe,setUniverse]=useState('immo');const [criteria,setCriteria]=useState('{}');const [error,setError]=useState('');async function load(){const r=await fetch('/api/alerts');if(r.ok)setItems((await r.json()).data??[])}useEffect(()=>{void load()},[]);async function create(){setError('');try{const c=JSON.parse(criteria||'{}');const r=await fetch('/api/alerts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,universe,criteria:c})});const d=await r.json();if(!r.ok)throw new Error(d.error);setTitle('');await load()}catch(e){setError(e instanceof Error?e.message:'Erreur')}}return <main className="wrap"><header className="header"><div><div className="brand">WinsWi · Alertes</div><div className="tag">Recevez les nouvelles correspondances</div></div><Link className="btn secondary" href="/dashboard">Mon espace</Link></header><section className="card formCard"><h1>Créer une alerte</h1><div className="formGrid"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Ex. F3 à Mostaganem"/><select value={universe} onChange={e=>setUniverse(e.target.value)}>{universes.map(u=><option key={u.id} value={u.id}>{u.name}</option>)}</select></div><textarea value={criteria} onChange={e=>setCriteria(e.target.value)} rows={5}/>{error&&<div className="notice error">{error}</div>}<button className="btn" onClick={()=>void create()} disabled={!title.trim()}>Créer l’alerte</button></section><section className="dashboardSection"><div className="sectionHead"><h2>Mes alertes</h2><span className="muted">{items.length}</span></div><div className="grid">{items.map(x=><div className="card" key={x.id}><div className="muted">{x.universe}</div><h3>{x.title}</h3><pre>{JSON.stringify(x.criteria,null,2)}</pre></div>)}</div></section></main>}
