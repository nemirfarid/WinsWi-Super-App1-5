# WinsWi 5.1.2 — Validation et diagnostic

## Source utilisée
Release: `WinsWi-Super-App1-5.1.2-Final-Complete`.

Le SHA-256 du ZIP fourni correspond au fichier `.sha256` associé.

## Contrôles effectués sur la source

- `node scripts/verify-install.mjs` : OK
- `node scripts/verify-source.mjs` : OK — 92 fichiers source, aucun import `@/` manquant
- `node scripts/contract-check.mjs` : OK — 12 universes et contrats IA/voix, paiements, préférences, opportunités, social et intelligence présents

## Limitation de cette validation

L'environnement d'analyse n'a pas terminé `npm install` dans le délai disponible. Par conséquent, `typecheck`, `lint` et `build` n'ont pas été déclarés comme validés ici. Ils doivent être exécutés sur le PC Windows avec Node 22/npm 10.

## Corrections préparées

- Ajout de `scripts/diagnose.mjs` pour exécuter les cinq validations dans l'ordre et s'arrêter à la première erreur.
- Ajout du script npm `diagnose`.
- Ajout de `INSTALL-WINDOWS.ps1` : sauvegarde l'actuel `C:\WinsWi`, crée `C:\WinsWi-5.1.2`, installe les dépendances puis exécute la validation complète.
- Aucun secret n'est ajouté.
- Aucun `package-lock.json` n'est fabriqué artificiellement.

## Universes

La source actuelle déclare 12 universes : ImmoWin, AutoWin, JobWin, MarketWin, BuildWin, AgriWin, TravelWin, EduWin, HealthWin, SportWin, FoodWin et DeliveryWin.

Les documents historiques qui parlent de 7 universes restent des documents d'anciennes étapes et ne doivent pas être utilisés comme état courant de la 5.1.2.
